//User reducer
export const moduleUser = "USER";

export const initialStateUser = {
  users: []
};

export const reducerUser = function(state, action) {
  switch (action.type) {
    default:
      return state;
  }
};
